spring:
  application:
    name: user-service
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
  servlet:
    context-path: /api

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET}
      expiration: 86400000
